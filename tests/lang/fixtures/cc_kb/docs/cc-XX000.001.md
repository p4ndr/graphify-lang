# XX spoke one

Self cc-XX000.001 is not an edge. See cc-YY100.000 and cc-ZZ999.000 (missing).
Run `scripts/tool.ps1:12` or `$CLAUDE_HOME/scripts/tool.ps1`; `scripts/none.ps1` does not exist.
Version cc-QQ100.000.2 is not a mention.

```powershell
`scripts/tool.ps1` in a fence is not a code path
```
